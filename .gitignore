# Compiled class files
*.class
target/

# Log files
*.log

# Package Files
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# IDE specific files
.idea/
*.iml
*.iws
*.ipr
.vscode/
.classpath
.project
.settings/

# OS specific
.DS_Store
Thumbs.db

# Docker
.dockerignore

# Maven
# mvnw, mvnw.cmd, and .mvn/ are intentionally committed so the project
# can be built without a system-wide Maven installation.

# Application specific
*.pid
spring.log
demo-calc-app/

# Local environment — contains secrets, never commit
.env
# .env.example is safe to commit (no real values)

# H2 file-based database (local persistence only)
data/*.mv.db
data/*.lock.db
data/*.trace.db
